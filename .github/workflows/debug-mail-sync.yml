name: Debug Gmail rental sync
# run: blob-persist

on:
  push:
    branches:
      - feature/admin-societa-iscritte
    paths:
      - .github/workflows/debug-mail-sync.yml

permissions:
  contents: read

jobs:
  check:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - name: Check branch deploy Gmail sync
        run: |
          python - <<'PY'
          import urllib.request
          url = "https://feature-admin-societa-iscritte--campionatiitalianicoastal2026.netlify.app/api/_tmp-mail-sync-diagnose-7f2c9d41"
          with urllib.request.urlopen(url, timeout=30) as response:
              print(response.read().decode("utf-8"))
          PY
