name: Merge admin settings sections

on:
  push:
    branches: [develop]
    paths:
      - .github/workflows/merge-admin-settings-20260912.yml

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: develop

      - name: Apply settings merge
        run: python .github/scripts/merge-admin-settings-20260912.py

      - name: Validate
        run: |
          node --check boat-booking-admin-list-ux.js
          git diff --check

      - name: Commit and cleanup
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add admin-prenotazioni-barche.html boat-booking-admin-list-ux.js
          git rm .github/workflows/merge-admin-settings-20260912.yml .github/scripts/merge-admin-settings-20260912.py
          git commit -m "Accorpa impostazioni area admin"
          git push origin HEAD:develop
