name: Matrix cell fill indicators

on:
  push:
    branches: [develop]
    paths:
      - .github/workflows/matrix-cell-fill-20260912.yml

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: develop

      - name: Apply matrix cell fill changes
        run: python .github/scripts/matrix-cell-fill-20260912.py

      - name: Validate
        run: |
          node --check boat-booking-admin.js
          git diff --check

      - name: Commit and cleanup
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add admin-prenotazioni-barche.html boat-booking-admin.js
          git rm .github/workflows/matrix-cell-fill-20260912.yml .github/scripts/matrix-cell-fill-20260912.py
          git commit -m "Aggiunge riempimento a ogni cella occupazione slot"
          git push origin HEAD:develop
