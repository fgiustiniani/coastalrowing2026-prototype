name: Admin report header and collapsible bookings

on:
  push:
    branches: [develop]
    paths:
      - .github/workflows/admin-report-header-20260912.yml

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: develop

      - name: Apply admin layout changes
        run: python .github/scripts/admin-report-header-20260912.py

      - name: Validate
        run: |
          node --check boat-booking-admin-list-ux.js
          node --check boat-booking-report-menu.js
          git diff --check

      - name: Commit and cleanup
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add admin-prenotazioni-barche.html boat-booking-admin-list-ux.js boat-booking-report-menu.js
          git rm .github/workflows/admin-report-header-20260912.yml .github/scripts/admin-report-header-20260912.py
          git commit -m "Riorganizza testata report admin"
          git push origin HEAD:develop
