name: Admin booking reporting enhancements

on:
  push:
    branches: [develop]
    paths:
      - .github/workflows/admin-reporting-20260912.yml

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: develop

      - name: Apply changes
        run: python .github/scripts/admin-reporting-20260912.py

      - name: Validate
        run: |
          node --check boat-booking-admin.js
          node --check boat-booking-admin-list-ux.js
          node --check boat-booking-pdf-export.js
          node --check boat-booking-admin-reports.js
          git diff --check

      - name: Commit and cleanup
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add admin-prenotazioni-barche.html boat-booking-admin.js boat-booking-admin-list-ux.js boat-booking-pdf-export.js boat-booking-admin-reports.js
          git rm .github/workflows/admin-reporting-20260912.yml .github/scripts/admin-reporting-20260912.py
          git commit -m "Migliora report prenotazioni admin"
          git push origin HEAD:develop
